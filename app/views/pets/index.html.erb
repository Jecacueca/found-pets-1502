<h1>Found Pets!</h1>

<% @pets.each do |pet| %>
  <h3><%= link_to pet.name, pet_path(pet) %></h3>
  <p><%= pet.address %> | <%= pet.found_on %> | <%= link_to '🗑️', pet_path(pet), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %></p>
<% end %>

<%= link_to "+ Pet", pets_new_path%>
